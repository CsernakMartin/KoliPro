<Window
    x:Class="KoliPro.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KoliPro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kolipro">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a2a6c" Offset="0" />
                <GradientStop Color="#b21f1f" Offset="0.5" />
                <GradientStop Color="#fdbb2d" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Padding="40"
                CornerRadius="12"
                Width="400">

            <StackPanel Spacing="20">
                <FontIcon Glyph="&#xE77B;" FontSize="48" Foreground="{ThemeResource SystemAccentColor}"/>

                <TextBlock Text="Bejelentkezés" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

                <TextBox x:Name="UsernameBox" Header="Felhasználónév" PlaceholderText="Felhasználónév"/>

                <PasswordBox x:Name="PasswordBox" Header="Jelszó" PlaceholderText="••••••••"/>

                <Button x:Name="LoginButton" 
                        Content="Beléptetés" 
                        Click="LoginButton_Click"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Height="40"/>

                <TextBlock x:Name="ErrorText" 
                           Foreground="Red" 
                           HorizontalAlignment="Center" 
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>